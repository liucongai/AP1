$(function(){

    $('.co_fx .course-nav-item').eq($.cookie("co_fx")).addClass('on');

    $('.co_cat .course-nav-item').eq($.cookie("co_cat")).addClass('on');

    $('.co_diff .course-nav-item').eq($.cookie("co_diff")).addClass('on');



});

